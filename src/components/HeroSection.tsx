const HeroSection = () => {
  // Hero intentionally minimal — let the 3D space background be the visual focus.
  return (
    <section
      aria-label="hero"
      className="relative min-h-[70vh] md:min-h-[80vh] flex items-center justify-center overflow-hidden"
    />
  );
};

export default HeroSection;